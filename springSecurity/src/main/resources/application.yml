spring:
  datasource:
    url: jdbc:h2:file:./data/auth_db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enable: true
      path: /h2-console